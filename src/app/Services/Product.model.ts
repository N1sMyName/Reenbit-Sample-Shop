export interface Product {
  title: string;
  description: string;
  rating: number;
  freshness: string;
  farm: string;
  delivery: string;
  inStock: number;
  activePrice: number;
  oldPrice: number;
  deliveryTime: number;
}
