export interface Product {
  title: string;
  description: string;
  rating: number;
  freshness: string;
  farm: string;
  devivery: string;
  inStock: number;
  activePrice: number;
  oldPrice: number;
  deviveryTime: number;
}
